name: ui_login_registration
description: algoriza_ui_login_registration_Task1

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"
dependencies:
  country_code_picker: ^2.0.2
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_svg: ^1.0.3
  smooth_page_indicator: ^1.0.0+2
  tbib_loading_transition_button_and_social: ^1.0.0

flutter_gen:
  output: lib/gen/
  line_length: 80

  # Optional
  integrations:
    flutter_svg: true
    flare_flutter: false
    rive: false

    fonts: null
    # Optional
    enabled: true

dev_dependencies:
  build_runner: ^2.1.11
  flutter_gen_runner: ^4.3.0

  flutter_test:
    sdk: flutter


  flutter_lints: ^2.0.0


flutter:


  uses-material-design: true

  assets:
    - assets/images/
    - assets/svg/
